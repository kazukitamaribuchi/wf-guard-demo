name: Deploy (secrets inherit)
on:
  push:
    branches: [main]

# 同じブランチの古い実行を自動キャンセルし、無駄な課金を防ぐ
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  call-deploy:
    uses: ./.github/workflows/reusable-deploy.yml
    secrets: inherit
